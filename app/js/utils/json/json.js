'use strict';

/**
 *
 * @param {string} fileName:文件名
 * @param {string} jsonData:json数据
 * @description 将json文件保存到upload文件夹
 */

const json = {
  save: (fileName, jsonData) => {
    $.getJson
  }
};

export default json;
